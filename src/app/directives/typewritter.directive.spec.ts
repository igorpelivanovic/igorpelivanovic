import { TypewritterDirective } from './typewritter.directive';

describe('TypewritterDirective', () => {
  it('should create an instance', () => {
    const directive = new TypewritterDirective();
    expect(directive).toBeTruthy();
  });
});
