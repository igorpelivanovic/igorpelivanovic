import { IntoSectionObserverDirective } from './into-section-observer.directive';

describe('IntoSectionObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new IntoSectionObserverDirective();
    expect(directive).toBeTruthy();
  });
});
