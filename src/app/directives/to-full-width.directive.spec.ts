import { ToFullWidthDirective } from './to-full-width.directive';

describe('ToFullWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ToFullWidthDirective();
    expect(directive).toBeTruthy();
  });
});
