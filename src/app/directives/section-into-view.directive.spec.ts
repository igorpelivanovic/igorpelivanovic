import { SectionIntoViewDirective } from './section-into-view.directive';

describe('SectionIntoViewDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionIntoViewDirective();
    expect(directive).toBeTruthy();
  });
});
